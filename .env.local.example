# Include the database name as the path on the URI, e.g.
#   mongodb+srv://user:pass@cluster.example.net/youquizz?retryWrites=true
# MONGODB_DB is optional — only set it to override the URI's path.
MONGODB_URI=mongodb://localhost:27017/youquizz
